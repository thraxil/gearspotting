---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gearspotting-app
  labels:
    app: gearspotting
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gearspotting
  template:
    metadata:
      labels:
        app: gearspotting
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - gearspotting
              topologyKey: kubernetes.io/hostname
      volumes:
        - name: config-volume
          secret:
            secretName: gearspotting-config
      initContainers:
        - image: <IMAGE>
          name: migrate
          command: [ "/run.sh", "migrate" ]
          envFrom:
            - secretRef:
                name: gearspotting-secret
            - configMapRef:
                name: gearspotting-config
        - image: <IMAGE>
          name: collectstatic
          command: [ "/run.sh", "collectstatic" ]
          envFrom:
            - secretRef:
                name: gearspotting-secret
            - configMapRef:
                name: gearspotting-config
        - image: <IMAGE>
          name: compress
          command: [ "/run.sh", "compress" ]
          envFrom:
            - secretRef:
                name: gearspotting-secret
            - configMapRef:
                name: gearspotting-config
      containers:
        - image: <IMAGE>
          name: gearspotting
          envFrom:
            - secretRef:
                name: gearspotting-secret
            - configMapRef:
                name: gearspotting-config
          ports:
            - containerPort: 8000
              name: gunicorn
---
apiVersion: v1
kind: Service
metadata:
  name: gearspotting
  labels:
    app: gearspotting
spec:
  type: NodePort
  selector:
    app: gearspotting
  ports:
    - port: 8000
      targetPort: 8000
      name: gunicorn
